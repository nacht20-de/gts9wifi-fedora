[Unit]
# Serialize behind the panel recovery: starting the ADSP concurrently with
# the pm_test suspend cycle froze the SoC outright (observed on hardware).
After=gts9wifi-panel-coldboot-recover.service
