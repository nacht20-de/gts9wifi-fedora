[Service]
# The remoteproc start write returns before the fastrpc misc device probes;
# wait for it so units ordered After=adsp-boot see /dev/fastrpc-adsp exist.
ExecStartPost=/bin/sh -c 'i=0; while [ $i -lt 60 ] && [ ! -e /dev/fastrpc-adsp ]; do sleep 1; i=$((i+1)); done'
